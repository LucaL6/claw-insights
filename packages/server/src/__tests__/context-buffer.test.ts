/**
 * Buffer logic has been migrated to TranscriptSink (DESIGN-063 P6b).
 * This file is kept as a placeholder — buffer tests now live in
 * transcript-sink.test.ts.
 *
 * See: sources/collectors/__tests__/transcript-sink.test.ts
 */
import { describe, expect,it } from 'vitest';

describe('context-buffer (migrated)', () => {
  it('buffer logic now lives in TranscriptSink', () => {
    expect(true).toBe(true);
  });
});
